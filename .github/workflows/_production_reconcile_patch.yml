name: production-reconcile-patch

on:
  push:
    branches:
      - codex/account-authority-reconciliation
    paths:
      - .github/workflows/_production_reconcile_patch.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          ref: codex/account-authority-reconciliation
          fetch-depth: 0
      - name: Apply exact source patch
        run: python scripts/_apply_production_reconcile_patch.py
      - name: Commit and self-delete
        run: |
          rm scripts/_apply_production_reconcile_patch.py .github/workflows/_production_reconcile_patch.yml
          git config user.name firmquant-agent
          git config user.email firmquant-agent@users.noreply.github.com
          git add -A
          git commit -m "feat: gate production account reconciliation"
          git push origin HEAD:codex/account-authority-reconciliation
