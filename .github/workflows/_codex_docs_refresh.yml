name: Apply current account authority docs

on:
  push:
    branches:
      - codex/account-authority-reconciliation
    paths:
      - .github/_codex_docs_refresh.py
      - .github/workflows/_codex_docs_refresh.yml

permissions:
  contents: write

jobs:
  docs:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          fetch-depth: 0
      - run: python .github/_codex_docs_refresh.py
      - run: rm .github/_codex_docs_refresh.py .github/workflows/_codex_docs_refresh.yml
      - run: |
          git diff --check
          git config user.name "firmquant-agent"
          git config user.email "firmquant-agent@users.noreply.github.com"
          git add -A
          git diff --cached --quiet && exit 1
          git commit -m "docs: document account authority and reconciliation"
          git push origin HEAD:codex/account-authority-reconciliation
