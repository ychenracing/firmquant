name: tmp-account-finalization-green
on:
  push:
    branches:
      - codex/account-authority-reconciliation
permissions:
  contents: write
jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          fetch-depth: 0
      - name: Apply exact finalization patch
        run: |
          python .tmp/patch_account_finalization_green.py
          git rm .tmp/patch_account_finalization_green.py
          git rm .github/workflows/tmp-account-finalization-green.yml
          git config user.name firmquant-agent
          git config user.email firmquant-agent@users.noreply.github.com
          git add src tests
          git commit -m "fix: finalize reconciled account state atomically"
          git push origin HEAD:codex/account-authority-reconciliation
